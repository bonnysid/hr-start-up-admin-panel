import { useNavigate } from 'react-router-dom';
import * as ST from './styled';

export const Navbar = () => {
  const navigate = useNavigate();

  return (
    <ST.Wrapper>

    </ST.Wrapper>
  );
}
